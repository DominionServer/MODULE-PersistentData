name: PersistentData
version: '${project.version}'
main: net.phonographman.persistentdata.command.TestCommand
api-version: 1.15
prefix: PD
authors: [ DominionServer, Lord_Bones ]
description: Writes to and reads from files to make them Persistent between restarts.
website: https://phonographman.net
commands:
  persist:
    description: Allows you to set and read data from a file
    usage: /<command>